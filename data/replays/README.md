# Replay input directory

Place local `.SC2Replay` files here before conversion. Replay archives are not
tracked by Git because expert datasets can be large and may have licensing or
distribution restrictions.
